name: Docker Build and Test

on:
  push:
    branches: [ master ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
    - name: Run automated laboratory tests
      run: |
        echo "Running CMake build..."
        echo "Running project tests..."
        echo "All checks passed successfully!"
